# Framework principal para interface web
streamlit>=1.28.0

# API do WhatsApp via Twilio
twilio>=8.5.0

# Requisições HTTP para API de CEP
requests>=2.31.0

# Banco de dados SQLite (já incluído no Python, mas listando para clareza)
# sqlite3 - Built-in Python module

# Dependências adicionais que podem ser úteis
python-dotenv>=1.0.0  # Para variáveis de ambiente (recomendado para credenciais)
pandas>=2.0.0         # Para manipulação de dados (útil para relatórios)
plotly>=5.15.0        # Para gráficos no painel admin (opcional)

# Dependências de desenvolvimento (opcional)
pytest>=7.4.0         # Para testes
black>=23.7.0         # Para formatação de código
flake8>=6.0.0         # Para linting